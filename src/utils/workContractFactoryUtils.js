export async function getContractsForClient(contractMethods, userAddress) {
  console.log("contractMethods", contractMethods);
  // const data = await contractMethods.tokensOfOwner(userAddress).call();
  // const uri = await contractMethods.tokenURI(data[0]).call();
  // console.log("uri", uri);
}

export async function newContract(contractMethods) {}

export async function getOpenContracts(contractMethods) {}

export async function assignWorker(contractMethods) {}
